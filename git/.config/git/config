[include]
  path = ~/.local-gitconfig

[core]
  pager = delta
  editor = nvim

[interactive]
  diffFilter = delta --color-only

[delta]
  navigate = true    # use n and N to move between diff sections

    # delta detects terminal colors automatically; set one of these to disable auto-detection
    # dark = true
    # light = true

[merge]
  conflictstyle = diff3

[diff]
  colorMoved = default
[commit]
  gpgsign = true
[user]
	email = jonas.hungershausen@gmail.com
	name = Jonas Hungershausen
	signingkey = /home/jonas/.ssh/id_ed25519.pub
[gpg]
	format = ssh
